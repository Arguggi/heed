-- | Moduled used when testing to check if the sql file matches the haskell definitions
--    and if the current heed database also matches.
module Heed.QueryTest
  (
  )
where

-- checkAuthTokenTable,
-- checkFeedInfoTable,
-- checkFeedItemTable,
-- checkPrefTable,
-- checkSubscriptionTable,
-- checkUnreadItemTable,
-- checkUserTable,

-- import Data.Int (Int64)
-- import qualified Heed.Database as DB
-- import qualified Opaleye as O

-- | Query 'DB.authTokenTable'
-- checkAuthTokenTable :: IO [Int64]
-- checkAuthTokenTable = O.countRows (O.selectTable DB.authTokenTable)
--
-- -- | Query 'DB.feedInfoTable'
-- checkFeedInfoTable :: IO [Int64]
-- checkFeedInfoTable = O.countRows (O.selectTable DB.feedInfoTable)
--
-- -- | Query 'DB.feedItemTable'
-- checkFeedItemTable :: IO [Int64]
-- checkFeedItemTable = O.countRows (O.selectTable DB.feedItemTable)
--
-- -- | Query 'DB.userPrefTable'
-- checkPrefTable :: IO [Int64]
-- checkPrefTable = O.countRows (O.selectTable DB.userPrefTable)
--
-- -- | Query 'DB.subscriptionTable'
-- checkSubscriptionTable :: IO [Int64]
-- checkSubscriptionTable = O.countRows (O.selectTable DB.subscriptionTable)
--
-- -- | Query 'DB.unreadItemTable'
-- checkUnreadItemTable :: IO [Int64]
-- checkUnreadItemTable = O.countRows (O.selectTable DB.unreadItemTable)
--
-- -- | Query 'DB.userTable'
-- checkUserTable :: IO [Int64]
-- checkUserTable = O.countRows (O.selectTable DB.userTable)
